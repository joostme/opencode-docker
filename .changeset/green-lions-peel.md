---
"opencode-docker": minor
---

Add the GitHub CLI to the Docker image so `gh` commands are available in the terminal and to agents, and document optional `GH_TOKEN` / `GITHUB_TOKEN` authentication for container startup.
